class CartItem {
  constructor(quantitiy, productPrice, productTitle, sum) {
    this.quantitiy = quantitiy;
    this.productPrice = productPrice;
    this.productTitle = productTitle;
    this.sum = sum;
  }
}
export default CartItem;
